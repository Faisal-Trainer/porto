# Implementation Plan: IP-2026-04-28-002
## Goal: Implementasi Keamanan API Token untuk MCP

### 🛠️ Proposed Changes

#### 1. [MODIFY] [config/services.php](file:///c:/xampp/htdocs/portofolio/config/services.php)
- **Perubahan**: Menambahkan konfigurasi untuk token MCP.
- **Isi**: `'mcp' => ['token' => env('MCP_TOKEN')]`.

#### 2. [MODIFY] [app/Http/Controllers/McpController.php](file:///c:/xampp/htdocs/portofolio/app/Http/Controllers/McpController.php)
- **Perubahan**: Menambahkan logika validasi di awal method `handle`.
- **Logic**: Jika header `X-MCP-Token` tidak cocok dengan `config('services.mcp.token')`, kembalikan `401 Unauthorized`.

#### 3. [ACTION] Update .env
- **Perubahan**: Menambahkan `MCP_TOKEN=rahasia_nexus_anda`.

### ✅ Verification Plan
1.  **Unauthorized Test**: Memanggil `/mcp` tanpa token dan memastikan mendapat error `401`.
2.  **Authorized Test**: Memanggil `/mcp` dengan token yang benar dan memastikan mendapat respon sukses.

---
*Generated by Nexus Orchestrator*
